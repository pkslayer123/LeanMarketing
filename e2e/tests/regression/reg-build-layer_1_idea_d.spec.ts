/**
 * Auto-generated regression test for MOC build-layer_1_idea_definition-1772555981549.
 * Verifies the fix for: Build feature: Layer 1 — Idea Definition
 * Changed files: app/auth/login/page.tsx
 * Generated by moc-auto-fix.js — do not edit manually.
 */

import { test, expect } from "../../fixtures/test";

test.describe("Regression: reg-build-layer_1_idea_d", () => {
  test.beforeEach(async ({ sim }) => {
    await sim.loginAsDeveloper();
    await sim.simulateAs({
      role: "user",
      name: "one-sentence",
    });
  });

  test("page loads without errors after fix", async ({ page, sim }) => {
    const consoleErrors: string[] = [];
    page.on("console", (msg) => {
      if (msg.type() === "error") {
        consoleErrors.push(msg.text());
      }
    });

    await sim.goto("/projects");
    await page.waitForLoadState("networkidle");

    // Verify page loaded
    const title = await page.title();
    expect(title).toBeTruthy();

    // Check no critical console errors
    const critical = consoleErrors.filter(
      (e) => !e.includes("Failed to fetch") && !e.includes("hydration")
    );
    expect(critical.length).toBe(0);

    await sim.recordGreenPass("reg-build-layer_1_idea_d", ["app/auth/login/page.tsx"]);
  });
});
